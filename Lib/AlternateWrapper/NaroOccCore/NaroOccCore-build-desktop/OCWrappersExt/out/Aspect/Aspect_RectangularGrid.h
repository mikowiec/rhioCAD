// File generated by CPPExt (Transient)
//
#ifndef _Aspect_RectangularGrid_OCWrappers_HeaderFile
#define _Aspect_RectangularGrid_OCWrappers_HeaderFile

// include the wrapped class
#include <Aspect_RectangularGrid.hxx>
#include "../Converter.h"

#include "Aspect_Grid.h"



namespace OCNaroWrappers
{




public ref class OCAspect_RectangularGrid : OCAspect_Grid {

protected:
  // dummy constructor;
  OCAspect_RectangularGrid(OCDummy^) : OCAspect_Grid((OCDummy^)nullptr) {};

public:

// constructor from native
OCAspect_RectangularGrid(Handle(Aspect_RectangularGrid)* nativeHandle);

// Methods PUBLIC

//! creates a new grid. By default this grid is not <br>
//!          active. <br>
//!          The first angle is given relatively to the horizontal. <br>
//!          The second angle is given relatively to the vertical. <br>
OCAspect_RectangularGrid(Quantity_Length aXStep, Quantity_Length aYStep, Quantity_Length anXOrigin, Quantity_Length anYOrigin, Quantity_PlaneAngle aFirstAngle, Quantity_PlaneAngle aSecondAngle, Quantity_PlaneAngle aRotationAngle);

//! defines the x step of the grid. <br>
 /*instead*/  void SetXStep(Quantity_Length aStep) ;

//! defines the y step of the grid. <br>
 /*instead*/  void SetYStep(Quantity_Length aStep) ;

//! defines the angle of the second network <br>
//!          the fist angle is given relatively to the horizontal. <br>
//!          the second angle is given relatively to the vertical. <br>
 /*instead*/  void SetAngle(Quantity_PlaneAngle anAngle1, Quantity_PlaneAngle anAngle2) ;


 /*instead*/  void SetGridValues(Quantity_Length XOrigin, Quantity_Length YOrigin, Quantity_Length XStep, Quantity_Length YStep, Quantity_PlaneAngle RotationAngle) ;

//! returns the point of the grid the closest to the point X,Y <br>
 /*instead*/  void Compute(Quantity_Length X, Quantity_Length Y, Quantity_Length& gridX, Quantity_Length& gridY) ;

//! returns the x step of the grid. <br>
 /*instead*/  Quantity_Length XStep() ;

//! returns the x step of the grid. <br>
 /*instead*/  Quantity_Length YStep() ;

//! returns the x Angle of the grid, relatively to the horizontal. <br>
 /*instead*/  Quantity_PlaneAngle FirstAngle() ;

//! returns the y Angle of the grid, relatively to the vertical. <br>
 /*instead*/  Quantity_PlaneAngle SecondAngle() ;


virtual /*instead*/  void Init() ;

~OCAspect_RectangularGrid()
{
  nativeHandle->Nullify();
  delete nativeHandle;
}

};

}; // OCNaroWrappers

#endif
